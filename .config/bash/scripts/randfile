#!/usr/bin/env bash
if [[ -z "$1" ]]; then
	echo "Usage: $0 <directory>"
	exit 1
fi

DIR="$1"

if [[ ! -d "$DIR" ]]; then
	echo "Directory does not exist: $DIR"
	exit 1
fi

find "$DIR" -type f | shuf -n 1
